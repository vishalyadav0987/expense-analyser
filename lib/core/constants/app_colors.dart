import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // PRIMARY
  static const Color primary = Color(0xFF4EDEA3);

  static const Color primaryDark = Color(0xFF38C98F);

  // BACKGROUND
  static const Color background = Color(0xFF08120E);

  static const Color backgroundSecondary = Color(0xFF0F1D17);

  // TEXT
  static const Color white = Colors.white;

  static const Color textPrimary = Colors.white;

  static const Color textSecondary = Color(0xFF9FB2AA);

  static const Color hint = Color(0xFF70827A);

  // GLASS
  static const Color glass = Color.fromRGBO(255, 255, 255, 0.04);

  static const Color border = Color.fromRGBO(255, 255, 255, 0.06);

  // STATUS
  static const Color success = Color(0xFF3DD598);

  static const Color error = Color(0xFFFF5A5F);

  // EXTRA
  static const Color blueGlow = Color(0xFFADC6FF);

  static const Color buttonText = Color(0xFF062014);

  static const Color cardBackground = Color(0xFF13211B);

  static const Color textMuted = Color(0xFF86948A);
}
